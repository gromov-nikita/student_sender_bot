package org.bsut.student_sender_bot.service.bot.survey;

import org.springframework.stereotype.Service;

import java.util.Map;
import java.util.concurrent.ConcurrentHashMap;

@Service
public class SurveyService {
    //private final Map<Long, SurveySession> userSessions = new ConcurrentHashMap<>();

    //public SurveySession getSession(Long chatId) {
    //    return userSessions.computeIfAbsent(chatId, SurveySession::new);
    //}

    //public void removeSession(Long chatId) {
    //    userSessions.remove(chatId);
    //}
}
